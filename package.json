{
  "name": "mother-ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "mastra dev",          // runs Mastra Dev Server (defaults to src/mastra)
    "build": "mastra build"
  },
  "dependencies": {
    "@ai-sdk/openai": "^2.0.20",
    "@mastra/core": "^0.5.0"
  },
  "devDependencies": {
    "mastra": "^0.5.0",           // CLI
    "typescript": "^5.5.4",
    "tsx": "^4.19.0",
    "@types/node": "^20.14.2"
  }
}
